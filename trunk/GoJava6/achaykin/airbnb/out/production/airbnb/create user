CREATE TABLE User (
userID INT AUTO_INCREMENT,
first_name CHAR(30) NOT NULL,
last_name CHAR(30) NOT NULL,
eMail CHAR(35) NOT NULL UNIQUE,
city CHAR(30),
host BOOLEAN DEFAULT 0,
PRIMARY KEY(userID)
);